<div id="anchor-2" class="anchor-2">
    {% for item in items %}
        <div{{ item.attributes.addClass('anchor__item') }}>
            <div class="anchor__item-inner">
                {{ item.content }}
            </div>
        </div>
    {% endfor %}
</div>